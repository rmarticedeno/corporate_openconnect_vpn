#!/usr/bin/expect
set USER [lindex $argv 0]
set PASS [lindex $argv 1]
set timeout 1
spawn ocpasswd -c /etc/ocserv/ocpasswd $USER
expect "Enter password:\r"
send -- "$PASS\r"
expect "Re-enter password:\r"
send -- "$PASS\r"
expect eof
